From 9ca10f0dc4f6bacd1ca94bc25e2b5e31ceb4adbe Mon Sep 17 00:00:00 2001
From: Aikar <aikar@aikar.co>
Date: Sat, 7 Oct 2017 20:43:19 -0400
Subject: [PATCH] Do not load chunks for light checks

Should only happen for blocks on the edge that uses neighbors light level
(certain blocks). In that case, there will be 3-4 other neighbors to get a light level from

diff --git a/src/main/java/net/minecraft/world/World.java b/src/main/java/net/minecraft/world/World.java
index 0f23cd7..081ceeb 100644
--- a/src/main/java/net/minecraft/world/World.java
+++ b/src/main/java/net/minecraft/world/World.java
@@ -755,6 +755,7 @@ public abstract class World implements IBlockAccess {
                 if (pos.getY() >= 256) {
                     pos = new BlockPos(pos.getX(), 255, pos.getZ());
                 }
+                if (!this.isBlockLoaded(pos)) return 0; // Paper
 
                 Chunk chunk = this.getChunkFromBlockCoords(pos);
                 return chunk.getLightSubtracted(pos, this.skylightSubtracted);
-- 
2.18.0

